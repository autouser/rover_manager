require 'spec_helper'

describe RoverManager do
  it 'has a version number' do
    expect(RoverManager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
